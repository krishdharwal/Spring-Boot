spring:
  application:
   name: Config-Server-App
  cloud:
    config:
      server:
        git:
          uri: https://github.com/krishdharwal/config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

